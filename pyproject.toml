[project]
name = "farmanimalsalarm"
version = "0.1.0"
description = "Farm animal alarm and species detection GUI and tools"
readme = "README.md"
requires-python = ">=3.8"

[tool.pytest.ini_options]
minversion = "6.0"
addopts = "-q"
testpaths = ["tests"]
